// import { selectVideoPlayerDomain } from '../selectors';

describe('selectVideoPlayerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
